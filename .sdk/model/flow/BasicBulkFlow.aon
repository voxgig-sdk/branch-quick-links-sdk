# BasicBulkFlow

main: kit: flow: BasicBulkFlow:
{
  entity: "bulk",
  kind: "basic",
  name: "BasicBulkFlow",
  step: [
    {
      input: {
        ref: "bulk_ref01"
      }

      match: {
        branch_key: "branch_key01"
      }

      op: "create"
    }
  ],
  "key$": "BasicBulkFlow"
}